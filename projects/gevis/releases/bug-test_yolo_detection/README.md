# Release Package

This package contains images and annotations for your project.

## Directory Structure

- `images/` - Contains all images organized by split (train/val/test)
- `labels/` - Contains all annotations in YOLO format
- `metadata/` - Contains configuration and statistics

## Usage

This dataset is ready to use with YOLO or other compatible frameworks.